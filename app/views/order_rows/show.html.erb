<p style="color: green"><%= notice %></p>

<%= render @order_row %>

<div>
  <%= link_to "Edit this order row", edit_order_row_path(@order_row) %> |
  <%= link_to "Back to orders", orders_path %> |
  <%= link_to "Add product to order", new_order_row_path(order_id: @order_row.order_id) %>

  <%= button_to "Destroy this order row", @order_row, method: :delete %>
</div>
