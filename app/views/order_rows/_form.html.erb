<%= form_with(model: order_row) do |form| %>
  <% if order_row.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order_row.errors.count, "error") %> prohibited this order_row from being saved:</h2>

      <ul>
        <% order_row.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :order_id, style: "display: block" %>
    <%= form.text_field :order_id, :value => params[:order_id], :readonly => true%>
  </div>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.collection_select(:product_id, Product.all, :id, :name, :prompt => true) %>
  </div>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
